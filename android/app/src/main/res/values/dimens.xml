<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Overridden on Android 12 and up by the radius the launcher itself
         uses, so the widget matches whatever shape its neighbours have. -->
    <dimen name="widget_write_note_radius">16dp</dimen>
</resources>
